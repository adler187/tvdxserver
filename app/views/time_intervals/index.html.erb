<% title 'Time Intervals' %>

<ul id='time_intervals' class='sortable'>
<% @time_intervals.each do |time_interval| %>
  <%= sortable_list_item time_interval.description, time_interval %>
<% end %>
</ul>

<%= sortable_element('time_intervals', :url => sort_time_intervals_path, :handle => 'handle') %>

<%= link_to 'New time_interval', new_time_interval_path %>